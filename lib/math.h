#ifndef __BSKMT_MATH_H__
#define __BSKMT_MATH_H__

int intpow(int, int);
int roundup(int, int);
int rounddown(int, int);
long pmod(long, long);

#define MAX(x,y) (x) > (y) ? (x) : (y)
#define MIN(x,y) (x) < (y) ? (x) : (y)

#endif
